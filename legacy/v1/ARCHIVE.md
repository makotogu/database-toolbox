# V1 源码快照

这里保存 2026-09-16 开始重构前的源码和已有未提交修改，未包含 data/、密钥、连接配置或构建缓存。

V1 的同步、分区创建、备份恢复、旧页面和测试均已从 V2 的编译/运行目录移入此处。根项目的 Maven 构建不会编译这个目录。

如需维护旧功能，可在此目录按原 README 使用 Java 8 构建。与 V2 使用不同数据目录，避免两个进程同时读写相同配置。

完整的本机工作区快照和原始 diff 另存于 `.local-backups/20260916-110930/`（不入 Git）。
