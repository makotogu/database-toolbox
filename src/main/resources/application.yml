server:
  address: 127.0.0.1
  port: 8080

spring:
  servlet:
    multipart:
      max-file-size: 128MB
      max-request-size: 256MB
  main:
    banner-mode: "off"
  jackson:
    serialization:
      write-dates-as-timestamps: false

toolbox:
  storage-root: data
